# Laravel Convention Rules

- **Routes**: Define web routes in outes/web.php using named route helpers (oute('services')).
- **Blade Directives**: Use @extends, @section, @include, @foreach, and @if.
- **Storage Assets**: Access uploaded media via sset('storage/' . ).
